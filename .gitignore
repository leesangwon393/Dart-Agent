.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
qdrant_data/
*.egg-info/
.DS_Store

# 원본 코퍼스 데이터(5.2GB raw XML) — 코드 저장소에는 넣지 않는다.
# 메타데이터(manifest.jsonl/universe.csv/README 등)는 가볍고 유용하니 유지.
corpus/raw/
corpus/universe.xlsx

# .claude 세션/스케줄 상태 파일 — 저장소 코드가 아님
.claude/

# 전체 70개사 GPU 임베딩 결과(2.8GB, 로컬 전용) — 코드 저장소에는 넣지 않는다.
gpu_embeddings/
gpu_embeddings.tar.gz

# Phase 13 재청킹/재임베딩 결과물(로컬 전용, 수 GB) — 코드 저장소에는 넣지 않는다.
chunks_v2/
gpu_embeddings_v2/
*.log
chunks_v2_build.log
gpu_embeddings_v2_stdout.log

# 팀원(Kim)에게 받은 전체 코퍼스 임베딩 결과(2026-08-27, 수 GB, 로컬 전용).
pipeline_kim_v2/
임베딩결과_v2_20260827.zip
